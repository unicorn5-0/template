person.forEach(({name}) => {
      if (name === personObject.name) {
        alert(`${newName} is already added to PhoneBook`)
      } else {
        setPerson(person.concat(personObject))
      }
    })